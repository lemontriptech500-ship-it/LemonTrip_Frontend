import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Visa Services & Travel Visa Assistance',
  description: 'Explore visa services and travel visa assistance with LemonTrip.',
}

export default function VisaLayout({ children }: { children: React.ReactNode }) {
  return children
}
